apiVersion: v1
kind: ConfigMap
metadata:
  name: infinite-config
  namespace: infinite-server26
data:
  INFINITE_VERSION: "26.1"
  NAYDOE_MODE: "autonomous"
  JESSICAI_MODE: "huntress"
  NAI_GAIL_ENABLED: "true"
  NIA_VAULT_ACTIVE: "true"
  SECURITY_LEVEL: "maximum"
  MERCY_MODE: "disabled"
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: web-ui-config
  namespace: infinite-server26
data:
  index.html: |
    <!DOCTYPE html>
    <html>
    <head>
      <title>Infinite Server26 - FORTRESS</title>
      <meta charset="utf-8">
      <meta name="viewport" content="width=device-width, initial-scale=1">
    </head>
    <body>
      <h1>∞ Infinite Server26 - FORTRESS</h1>
      <p>Web UI deployed via Kubernetes</p>
      <p>Version: 26.1</p>
    </body>
    </html>
